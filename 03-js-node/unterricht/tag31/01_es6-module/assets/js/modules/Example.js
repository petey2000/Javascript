const Example = () => {
  // === DOM & VARS ===
  const DOM = {};

  // === INIT =========

  const init = () => {
    console.log('init Example');
  };

  // === EVENTS / XHR =======

  // === FUNCTIONS ====

  init();
};

export default Example;
