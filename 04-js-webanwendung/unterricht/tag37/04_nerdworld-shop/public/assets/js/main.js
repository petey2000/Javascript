'use strict';

(() => {
  // === DOM & VARS ===
  const DOM = {};

  // === INIT =========

  const init = () => {
    console.log('init');
  };

  // === EVENTS / XHR =======

  // === FUNCTIONS ====

  init();
})();
