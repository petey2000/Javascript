'use strict';

(() => {
  // === DOM & VARS ===
  const DOM = {};

  // === INIT =========

  const init = () => {
    console.log('init JS');
  };

  // === EVENTS / XHR =======

  // === FUNCTIONS ====

  init();
})();
